import { css } from "@emotion/css"
import { elements } from "./styled-record"

export const blogListStyles = css`
  margin: 2rem auto;
  justify-items: center;
  width: 100%;
  align-self: center;
  border-bottom: 1px solid ${elements.p};
`
